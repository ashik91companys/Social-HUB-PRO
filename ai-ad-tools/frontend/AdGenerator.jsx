REACT component for a simple Ad copy generator that calls the backend /api/generate-text endpoint.

Integrate into your dashboard; this is a minimal example and expects your app to proxy /api to the backend service.
